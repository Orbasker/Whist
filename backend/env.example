# Database (DATABASE_URL is used by the app and by Alembic for the current stage)
#
# Dev (default): local PostgreSQL - use .env with:
DATABASE_URL=postgresql://postgres@localhost:5432/whist_db
#
# Staging/Prod: Neon PostgreSQL - set in deployment env or .env.prod:
#   Create a project at https://neon.tech, copy the connection string.
#   Format: postgresql://USER:PASSWORD@ep-XXX.region.aws.neon.tech/neondb?sslmode=require
# DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require
#
# SQLite (optional, no Postgres needed):
# DATABASE_URL=sqlite:///./whist.db

# API
API_V1_PREFIX=/api/v1
PROJECT_NAME=Whist Game API

# CORS
CORS_ORIGINS=http://localhost:4200,http://localhost:3000

# better-auth (see docs/plan/authentication-architecture.md)
# Auth runs as a separate Node service; FastAPI verifies JWTs and can proxy /api/auth/*.
# BETTER_AUTH_URL: Base URL of the Better Auth API (e.g. http://localhost:3000/api/auth).
#   When set, requests to /api/auth/* are proxied to this URL.
# AUTH_JWT_SECRET: Same value as BETTER_AUTH_SECRET in the auth service (min 32 chars).
#   Used by FastAPI to verify JWTs on protected routes. Keep secret.
# BETTER_AUTH_URL=http://localhost:3000/api/auth
# AUTH_JWT_SECRET=your-32-char-minimum-secret-shared-with-auth-service

# Supabase (Phase 2 - not used in Phase 1)
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
# SUPABASE_JWT_SECRET=
