# Database (DATABASE_URL is used by the app and by Alembic for the current stage)
#
# Dev (default): local PostgreSQL - use .env with:
DATABASE_URL=postgresql://postgres@localhost:5432/whist_db
#
# Staging/Prod: Neon PostgreSQL - set in deployment env or .env.prod:
#   Create a project at https://neon.tech, copy the connection string.
#   Format: postgresql://USER:PASSWORD@ep-XXX.region.aws.neon.tech/neondb?sslmode=require
# DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require
#
# SQLite (optional, no Postgres needed):
# DATABASE_URL=sqlite:///./whist.db

# API
API_V1_PREFIX=/api/v1
PROJECT_NAME=Whist Game API

# CORS
CORS_ORIGINS=http://localhost:4200,http://localhost:3000

# Neon Auth — Get these from your Neon project dashboard
# Neon Auth is integrated with Neon PostgreSQL, so no separate service is needed.
# The frontend communicates directly with Neon Auth endpoints.
#
# NEON_AUTH_JWKS_URL = JWKS URL from Neon Auth settings
#   Get this from your Neon project dashboard → Users → Configuration → JWKS URL
#   Format: https://ep-xxx.neonauth.region.aws.neon.tech/neondb/auth/.well-known/jwks.json
#   FastAPI uses this to verify JWTs issued by Neon Auth.
#
# Example:
# NEON_AUTH_JWKS_URL=https://ep-shiny-voice-agz9vcbc.neonauth.c-2.eu-central-1.aws.neon.tech/neondb/auth/.well-known/jwks.json

# Supabase (Phase 2 - not used in Phase 1)
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
# SUPABASE_JWT_SECRET=
