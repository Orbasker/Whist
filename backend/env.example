# Database (DATABASE_URL is used by the app and by Alembic for the current stage)
#
# Dev (default): local PostgreSQL - use .env with:
DATABASE_URL=postgresql://postgres@localhost:5432/whist_db
#
# Staging/Prod: Neon PostgreSQL - set in deployment env or .env.prod:
#   Create a project at https://neon.tech, copy the connection string.
#   Format: postgresql://USER:PASSWORD@ep-XXX.region.aws.neon.tech/neondb?sslmode=require
# DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require
#
# SQLite (optional, no Postgres needed):
# DATABASE_URL=sqlite:///./whist.db

# API
API_V1_PREFIX=/api/v1
PROJECT_NAME=Whist Game API

# CORS
CORS_ORIGINS=http://localhost:4200,http://localhost:3000

# better-auth — YOU define these; they are not from the better-auth website or a dashboard.
# Our backend does not run the auth server. A separate Node app (using the better-auth
# library) runs the auth server. You configure that Node app and then tell FastAPI where
# it is and what secret it uses.
#
# BETTER_AUTH_URL = Where your auth server is running (the Node app you run).
#   Example: if your Node auth app runs at http://localhost:3000 and mounts better-auth
#   at /api/auth, set BETTER_AUTH_URL=http://localhost:3000/api/auth.
#   FastAPI will proxy /api/auth/* requests to this URL. If you haven't set up the Node
#   auth app yet, leave this unset; /api/auth/* will return 503.
#
# AUTH_JWT_SECRET = A secret string (min 32 chars) that you generate yourself.
#   Generate one:  openssl rand -base64 32
#   Put the SAME value in two places:
#     1. In your Node auth app's env as BETTER_AUTH_SECRET (see better-auth Node docs).
#     2. Here as AUTH_JWT_SECRET so FastAPI can verify JWTs the auth server signs.
#   There is no "JWT" or "secret" field on better-auth.com — you create it and set it in both apps.
#
# BETTER_AUTH_URL=http://localhost:3000/api/auth
# AUTH_JWT_SECRET=<paste the same secret you set in the Node auth app>

# Supabase (Phase 2 - not used in Phase 1)
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
# SUPABASE_JWT_SECRET=
